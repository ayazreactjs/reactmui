import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#ff3333',
    },
    secondary: {
      main: '#33ff33',
    },
  },
});
export default theme;
